# 电商平台 - 商品列表页与商品详情页系统

一个现代化的电商平台，包含商品列表页和商品详情页两个核心页面。系统采用 React 18 + TypeScript 构建，使用 shadcn/ui 作为 UI 组件库，Zustand 进行状态管理，React Router 处理路由，Mock.js 模拟后端数据。

## ✨ 功能特性

### 商品浏览
- ✅ 响应式商品网格布局（支持 1-4 列自适应）
- ✅ 商品卡片悬停动画效果
- ✅ 图片懒加载和容错处理
- ✅ 骨架屏加载状态
- ✅ 空状态友好提示

### 筛选与搜索
- ✅ 价格区间筛选（滑块组件）
- ✅ 商品分类筛选
- ✅ 关键词搜索（支持商品名称和描述）
- ✅ 防抖搜索优化（500ms）
- ✅ 多条件组合筛选
- ✅ 移动端抽屉式筛选面板

### 排序与分页
- ✅ 价格升序/降序排序
- ✅ 评分排序
- ✅ 分页导航
- ✅ 筛选/排序时自动重置分页

### 购物车
- ✅ 添加商品到购物车
- ✅ 购物车数量徽章
- ✅ 购物车抽屉面板
- ✅ 商品数量增减
- ✅ 删除商品
- ✅ 总价自动计算
- ✅ localStorage 持久化

### 商品详情
- ✅ 商品完整信息展示
- ✅ 图片轮播
- ✅ 规格参数展示
- ✅ 加入购物车/立即购买
- ✅ 光晕按钮交互效果

### 用户体验
- ✅ Toast 通知反馈
- ✅ 错误边界处理
- ✅ 404 页面
- ✅ 响应式设计（支持手机、平板、桌面）
- ✅ 键盘导航支持
- ✅ ARIA 无障碍属性

### 性能优化
- ✅ React.memo 组件优化
- ✅ useMemo 计算缓存
- ✅ useCallback 回调稳定
- ✅ 图片懒加载
- ✅ 代码分割（React.lazy）

## 🛠️ 技术栈

### 核心框架
- **React 18.x** - 前端框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具

### UI 组件
- **shadcn/ui** - 基于 Radix UI 的组件库
- **Tailwind CSS** - 原子化 CSS 框架
- **Lucide React** - 图标库
- **Sonner** - Toast 通知组件
- **Vaul** - 抽屉组件

### 状态管理
- **Zustand** - 轻量级状态管理
- **zustand/middleware** - 持久化中间件

### 路由
- **React Router v7** - 客户端路由

### 数据模拟
- **Mock.js** - 模拟后端数据

### 工具库
- **clsx** - 动态类名
- **tailwind-merge** - Tailwind 类名合并
- **lodash.debounce** - 防抖函数

### 开发工具
- **ESLint** - 代码检查
- **Prettier** - 代码格式化
- **Vitest** - 单元测试
- **@testing-library/react** - React 组件测试
- **fast-check** - 属性基础测试

## 📦 快速开始

### 环境要求

- Node.js >= 16.x
- npm >= 8.x

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173 查看应用

### 运行测试

```bash
# 运行所有测试
npm run test

# 监听模式运行测试
npm run test:watch

# 测试 UI 界面
npm run test:ui
```

### 代码检查和格式化

```bash
# 运行 ESLint 检查
npm run lint

# 自动修复 ESLint 问题
npm run lint:fix

# 格式化代码
npm run format

# 检查代码格式
npm run format:check
```

### 构建生产版本

```bash
npm run build
```

### 预览生产构建

```bash
npm run preview
```

## 📁 项目结构

```
src/
├── components/           # 组件目录
│   ├── ui/              # shadcn/ui 基础组件
│   │   ├── button.tsx
│   │   ├── card.tsx
│   │   ├── drawer.tsx
│   │   ├── input.tsx
│   │   ├── slider.tsx
│   │   └── ...
│   ├── common/          # 通用组件
│   │   ├── MainLayout.tsx      # 主布局
│   │   ├── Header.tsx          # 导航栏
│   │   ├── Footer.tsx          # 页脚
│   │   ├── EmptyState.tsx      # 空状态
│   │   ├── ImageWithFallback.tsx  # 容错图片
│   │   ├── GlowButton.tsx      # 光晕按钮
│   │   ├── ProductSkeleton.tsx # 骨架屏
│   │   └── ErrorBoundary.tsx   # 错误边界
│   ├── product/         # 商品相关组件
│   │   ├── ProductCard.tsx     # 商品卡片
│   │   ├── ProductGrid.tsx     # 商品网格
│   │   ├── ProductFilter.tsx   # 筛选组件
│   │   ├── ProductSort.tsx     # 排序组件
│   │   └── ProductPagination.tsx  # 分页组件
│   └── cart/            # 购物车组件
│       ├── CartBadge.tsx       # 购物车徽章
│       ├── CartDrawer.tsx      # 购物车抽屉
│       └── CartItem.tsx        # 购物车项
├── pages/               # 页面组件
│   ├── ProductListPage.tsx     # 商品列表页
│   ├── ProductDetailPage.tsx   # 商品详情页
│   └── NotFoundPage.tsx        # 404 页面
├── hooks/               # 自定义 Hooks
│   ├── useCart.ts              # 购物车 Hook
│   ├── useProducts.ts          # 商品数据 Hook
│   ├── useDebounce.ts          # 防抖 Hook
│   └── useMediaQuery.ts        # 媒体查询 Hook
├── store/               # Zustand 状态管理
│   ├── cartStore.ts            # 购物车状态
│   └── productStore.ts         # 商品状态
├── services/            # API 服务层
│   └── mockApi.ts              # Mock API
├── utils/               # 工具函数
│   ├── filters.ts              # 筛选工具
│   ├── sorting.ts              # 排序工具
│   └── calculations.ts         # 计算工具
├── constants/           # 常量定义
│   └── index.ts
├── types/               # TypeScript 类型
│   └── index.ts
└── styles/              # 全局样式
    ├── globals.css
    └── glow-button.module.css
```

## 🔧 配置说明

### 路径别名

项目配置了 `@/` 作为 `src/` 的别名：

```typescript
// 使用别名导入
import { Product } from '@/types';
import { useCart } from '@/hooks/useCart';
import { Button } from '@/components/ui/button';
```

### 环境变量

创建 `.env.development` 文件配置开发环境变量：

```env
# Mock API 延迟时间（毫秒）
VITE_MOCK_DELAY=500
```

### TypeScript 配置

项目使用严格的 TypeScript 配置，确保类型安全：

```json
{
  "compilerOptions": {
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true
  }
}
```

## 📝 开发规范

### 代码风格

- 使用 **TypeScript** 进行类型检查
- 使用 **ESLint** 进行代码检查
- 使用 **Prettier** 进行代码格式化
- 遵循 **React Hooks** 最佳实践

### 组件开发

- 使用函数式组件 + Hooks
- 使用 `React.memo` 优化性能
- 使用 `useMemo` 和 `useCallback` 缓存计算和回调
- 为复杂函数添加 JSDoc 注释

### 样式开发

- 使用 **Tailwind CSS** 原子化类名
- 使用 `cn()` 工具函数合并类名
- 响应式设计优先（mobile-first）

### 状态管理

- 使用 **Zustand** 管理全局状态
- 使用 `persist` 中间件实现持久化
- 派生状态使用计算函数而非存储

### 测试

- 单元测试覆盖工具函数和 Hooks
- 组件测试使用 React Testing Library
- 属性基础测试使用 fast-check
- 每个属性测试至少运行 100 次迭代

## 🎯 核心功能实现

### 购物车持久化

购物车使用 Zustand 的 persist 中间件实现 localStorage 持久化：

```typescript
export const useCartStore = create<CartStore>()(
  persist(
    (set, get) => ({
      items: [],
      addItem: (product) => {
        // 添加逻辑
      },
      // ...
    }),
    {
      name: 'cart-storage',
    }
  )
);
```

### 防抖搜索

搜索输入使用 useDebounce Hook 实现 500ms 防抖：

```typescript
const debouncedKeyword = useDebounce(keyword, 500);
```

### 响应式筛选

移动端使用抽屉，桌面端使用侧边栏：

```typescript
const isMobile = useMediaQuery('(max-width: 768px)');
```

### 图片容错

图片加载失败时自动显示备用图片：

```typescript
<ImageWithFallback
  src={product.image}
  alt={product.name}
  fallback="/images/placeholder.png"
/>
```

## 📚 相关文档

- [项目初始化说明](doc/项目初始化说明.md)
- [需求文档](.kiro/specs/ecommerce-product-pages/requirements.md)
- [设计文档](.kiro/specs/ecommerce-product-pages/design.md)
- [任务列表](.kiro/specs/ecommerce-product-pages/tasks.md)

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 License

ISC

## 👥 作者

电商平台开发团队

---

**注意**: 本项目使用 Mock.js 模拟后端数据，生产环境需要对接真实 API。
